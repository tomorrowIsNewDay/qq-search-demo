export interface SearchResult {
    code: number;
    qq: string;
    name: string;
    qlogo: string;
    lvzuan: object;
  }
  
export interface SerachError {
    visible: boolean;
    tip: string
}
